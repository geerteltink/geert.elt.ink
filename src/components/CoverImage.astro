---
export interface Props {
  src: string;
  alt: string;
  [attr: string]: any;
}
// const { src, ...attrs } = Astro.props ;
---

<!--Picture src={src} formats={['avif', 'webp']} widths={[400, 800, 1280, 1920]} sizes="100vw" {...attrs} /-->

<style>
  img, picture :global(img), svg {
    border-radius: 0.5rem;
  }
</style>
